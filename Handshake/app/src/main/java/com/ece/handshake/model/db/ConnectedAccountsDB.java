package com.ece.handshake.model.db;

import android.content.Context;

import com.ece.handshake.model.data.SMAccount;

public class ConnectedAccountsDB {

    public void addAccount(SMAccount connectedAccount) {
        //Just write the implementation, If you need access to objects like Context then just pass it in as a parameter

        //Basically want to create a table that stores the fields in connectedAccount object.



    }
}
