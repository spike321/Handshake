package com.ece.handshake.model.service;

public class ConnectToPlatformService {
    // Just create methods here for each platform (Instagram, Twitter, etc...)
    // I'll figure out a way to use this soon. If you need certain objects like Context class then just pass it in as parameter for now.
    //It doesn't have to work I just need something to work with for testing.

}
